function init()
    --message.setHandler("objectScanned", function(...) onObjectScanned(...) end)
end

function addResearch(message, isLocal, researchName, researchLevel)

end

function setResearch(message, isLocal, researchName, researchLevel)

end

function removeResearch(message, isLocal, researchName)
    
end

function hasResearch(message, isLocal, researchName, researchLevel)

end

